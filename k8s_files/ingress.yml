apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: covid-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/default-backend: "covid-frontend:3000"
spec:
  rules:
    - http:
        paths:
          - path: /covid
            pathType: Prefix
            backend:
              service:
                name: covid-frontend
                port:
                  number: 3000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: covid-api
                port:
                  number: 8080
